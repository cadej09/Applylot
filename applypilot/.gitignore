# User data (NEVER commit)
profile.json
resume.txt
resume.pdf
*.env
.env.*
!.env.example

# Runtime artifacts
*.db
tailored_resumes/
cover_letters/
chrome-workers/
apply-workers/
apply_logs/
logs/

# MCP configs (per-machine)
.mcp*.json

# Virtual env (per-machine — never sync across OSes)
.venv/
venv/

# Generated tracker outputs
job_tracker.xlsx
job_tracker.csv

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# === job-ops data separation (see DATA_SYNC.md) ===
# Job data lives in its own private repo cloned at ~/.applypilot
# (github.com/your-github-user/applypilot-data). Never tracked in the code repo.
.applypilot/
*.db-wal
*.db-shm

# Local credential/data dir used by sandbox sessions (gmail OAuth lives here)
.applypilot/
